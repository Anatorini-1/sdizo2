#include "MinHeap.h"
#include <iostream>
void MinHeap::add(int key, int val)
{
	//if (index == len) {
	//	std::cout << "Overflow. Can not insert new node\n";
	//	return;
	//}
	//index++;
	//int i = index - 1;
	//arr[i] = new node{ key,val };
	//heapify(i);

}

void MinHeap::init(int size)
{
	//len = size;
	//index = 0;
	//arr = new node*[size];
}

void MinHeap::heapify(int i)
{
	//while (i != 0 && arr[parent(i)] > arr[i]) {
	//	swap(i, parent(i));
	//	i = parent(i);
	//}
}
